import React from 'react'

const SecoundaryTitle = ({ head_title }) => {
  return (
    <div className="text-primary max-w-xl bg-opacity-100 text-center md:text-start font-montserrat text-[25px] md:text-[25px] xl:text-[38px]  font-semibold  uppercase">
      <h1>{head_title}</h1>
    </div>
  )
}

export default SecoundaryTitle 
