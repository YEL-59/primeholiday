import React from 'react'

const SecoundaryTitle = ({ head_title }) => {
  return (
    <div className="text-primary max-w-xl bg-opacity-100 text-start  font-montserrat text-[20px] md:text-[25px] xl:text-[38px]  font-semibold  uppercase">
      <h1>{head_title}</h1>
    </div>
  )
}

export default SecoundaryTitle 
