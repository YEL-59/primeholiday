import React from 'react'

const SecoundaryTitle = ({ head_title }) => {
  return (
    <div className="text-primary bg-opacity-100 text-start font-montserrat text-[45px] font-semibold leading-[60px] w-7xl tracking-[1.069px] uppercase">
      <h1>{head_title}</h1>
    </div>
  )
}

export default SecoundaryTitle
